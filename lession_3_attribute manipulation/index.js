/* in javascript we can see the attribute by this way 

    const lookAttribute = document.querySelector(`a`).getAttribute(`href`);
    console.log(lookAttribute);

*/

/*in jquery we can see by this way */

$(`a`).attr(`href`);


